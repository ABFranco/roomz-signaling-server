import React, { useState } from 'react';

function Grid(props) {
  const [videos, setVideos] = useState([]])

  return (
    <div className="grid-container">
      {videos.map((v, index) => { v })}
    </div>
  )
}